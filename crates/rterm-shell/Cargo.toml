[package]
name = "rterm-shell"
description = "Native WebView wrapper + local PTY + localhost WebSocket bridge"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
